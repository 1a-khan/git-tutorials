# GIT lernen durch praxis
Ich möchte folgende Befehle testen. 
- git rebase
- git merge
- git fetch
- git pull
- git remote add origin ...
- Änderung direkt in main (Konflikt-Test)
- ein neuer Punkt aus feature-branch
